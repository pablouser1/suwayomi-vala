using Gtk 4.0;
using Adw 1;

template $HomePage: Adw.NavigationPage {
    title: "Suwayomi";

    Adw.ToolbarView {
        [top]
        Adw.HeaderBar {}

        ScrolledWindow {
            Adw.ViewStack view_stack {
                enable-transitions: true;
            }
        }

        [bottom]
        Adw.ViewSwitcher {
            stack: view_stack;
            policy: narrow;
        }
    }
}
